export type coockieUser = {
    name: string;
    token: string;
};

export type APICharacter = {
    id: string;
    name: string;
    image: string;
    house: string;
    estado: string;
};

export type APISpell = {
    id: string;
    name: string;
    description: string;
};
